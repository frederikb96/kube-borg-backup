{{- $namespaces := dict -}}
{{- range .Values.apps -}}
  {{- $_ := set $namespaces .namespace true -}}
{{- end -}}

---
# ClusterRole for reading cluster-scoped resources (StorageClasses)
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "kube-borg-backup.serviceAccountName" . }}-clusterrole
  labels:
    {{- include "kube-borg-backup.labels" . | nindent 4 }}
rules:
  - apiGroups: ["storage.k8s.io"]
    resources: ["storageclasses"]
    verbs: ["get","list"]

{{- range $namespace := keys $namespaces }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kube-borg-backup.serviceAccountName" $ }}
  namespace: {{ $namespace }}
  labels:
    {{- include "kube-borg-backup.labels" $ | nindent 4 }}
---
# ClusterRoleBinding for reading StorageClasses
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "kube-borg-backup.serviceAccountName" $ }}-{{ $namespace }}-clusterrolebinding
  labels:
    {{- include "kube-borg-backup.labels" $ | nindent 4 }}
roleRef:
  kind: ClusterRole
  name: {{ include "kube-borg-backup.serviceAccountName" $ }}-clusterrole
  apiGroup: rbac.authorization.k8s.io
subjects:
  - kind: ServiceAccount
    name: {{ include "kube-borg-backup.serviceAccountName" $ }}
    namespace: {{ $namespace }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "kube-borg-backup.serviceAccountName" $ }}-role
  namespace: {{ $namespace }}
  labels:
    {{- include "kube-borg-backup.labels" $ | nindent 4 }}
rules:
  - apiGroups: ["snapshot.storage.k8s.io"]
    resources: ["volumesnapshots"]
    verbs: ["create","get","list","delete","patch","watch"]
  - apiGroups: [""]
    resources: ["persistentvolumeclaims"]
    verbs: ["create","get","list","delete","patch","watch"]
  - apiGroups: [""]
    resources: ["pods","pods/log","pods/exec"]
    verbs: ["create","get","list","delete","patch","watch"]
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["create","get","list","delete"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["get","list","watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "kube-borg-backup.serviceAccountName" $ }}-rolebinding
  namespace: {{ $namespace }}
  labels:
    {{- include "kube-borg-backup.labels" $ | nindent 4 }}
roleRef:
  kind: Role
  name: {{ include "kube-borg-backup.serviceAccountName" $ }}-role
  apiGroup: rbac.authorization.k8s.io
subjects:
  - kind: ServiceAccount
    name: {{ include "kube-borg-backup.serviceAccountName" $ }}
    namespace: {{ $namespace }}
{{- end }}
